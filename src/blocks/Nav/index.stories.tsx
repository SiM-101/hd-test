import React from "react";
import { Nav } from ".";

export default {
	title: "Blocks / Navigation",
	component: Nav,
};

export const Navigation = () => <Nav />;

Navigation.parameters = {
	layout: "fullscreen",
};
